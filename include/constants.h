#pragma once

#include <stdint.h>

constexpr uint8_t NUM_VOICES(4);

constexpr uint8_t SCREEN_WIDTH(128);
constexpr uint8_t SCREEN_HEIGHT(32);
constexpr uint8_t OLED_RESET(4);
constexpr uint8_t SCREEN_ADDRESS(0x3C);

constexpr uint16_t DAC_TIMER_MICROS(45);
constexpr uint16_t ENC_TIMER_MICROS(250);
